@model CISApps.Models.Home.LoginThaiD

@{
    Layout = "_BlankLayout";
    ViewData["Title"] = "Home Page";
    var error = Model.error;
}
<div class="bg-logo">
    <div class="d-flex justify-content-center align-items-center min-vh-100">
        <div class="p-5 bg-white rounded shadow">
            <form asp-action="Index" method="post">
                <div class="text-center h1">
                    CIS Apps                
                </div>
                <div class="bg-logo-key">
                    <br/><br/><br/><br/><br/><br/>
                </div>
                <div class="m-4 p-2 text-center">
                    @if (!string.IsNullOrEmpty(error))
                    {
                    <div class="alert alert-danger text-center h5">
                        @error
                    </div>
                    }
                    <div class="mx-auto" style="width: fit-content">
                        <p class="h3 text-center">บริการตรวจสอบข้อมูล</p>
                        <a href="http://web-app.bora.dopa.go.th/auth?state=cisapp" class="btn btn-outline-primary">เข้าสู่ระบบด้วย ThaID</a>
                    </div>                            
                </div>
            </form>
        </div>
    </div>
    @* <div class="col-sm-10 col-md-8 col-xl-6 col-xxl-6 mx-auto rounded border border-danger">
        <form asp-action="Index" method="post">
            <div class="text-center text-white h1">
                CIS Apps <br />
                
            </div>
            <div class="card  mt-4 p-3 rounded border-warning">
                <div class="card-body  p-0">
                    <div class="row">
                        <div class="bg-logo-key col-md-5 col-xl-5 col-xxl-6 my-6">
                            <br/><br/><br/><br/><br/><br/>
                        </div>
                        <div class="col m-4 p-2">
                            @if (!string.IsNullOrEmpty(error))
                            {
                            <div class="alert alert-danger text-center h5">
                                @error
                            </div>
                            }
                            <div class="mx-auto" style="width: fit-content">
                                <p class="h3 text-center">บริการตรวจสอบข้อมูล</p>
                                <a href="http://web-app.bora.dopa.go.th/auth?state=cisapp" class="btn btn-outline-primary">เข้าสู่ระบบด้วย ThaID</a>
                            </div>                            
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div> *@
</div>